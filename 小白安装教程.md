# M3H 手势导航 — 小白安装教程（图文版）

> 本教程面向**完全没用过命令行**的新手。全程跟着做，大约 **10 分钟**搞定。
>
> 如果嫌命令行麻烦，本仓库提供「**一键安装.bat**」，配置好 ADB 和 USB 调试后双击即可，所有步骤自动完成。本教程教你从头配置。

---

## 📋 整体流程（4 大步）

| 步骤 | 做什么 | 大概耗时 |
|---|---|---|
| ① | 下载本项目的 APK 和安装脚本 | 1 分钟 |
| ② | 在电脑上安装 ADB 工具 | 2 分钟 |
| ③ | 在随身WiFi上开启 USB 调试并连电脑 | 3 分钟 |
| ④ | 双击「一键安装.bat」，全自动完成 | 1 分钟 |

装完之后，随身WiFi 屏幕底部就会出现手势条，从屏幕最下边往上滑就能回桌面。

---

## 第 ① 步：下载项目文件

### 方式 A：只下载必需文件（推荐，最快）

下载下面两个文件，放到**同一个文件夹**里（比如桌面上新建一个 `GestureNav` 文件夹）：

| 文件 | 下载地址 |
|---|---|
| **GestureNav.apk**（应用本体） | [点此下载](./GestureNav.apk) |
| **一键安装.bat**（自动安装脚本） | [点此下载](./一键安装.bat) |

> 💡 **下载方法**：点上面的链接 → 页面右上角有个 **「Download raw」** 或 **「⋯」→ Download** 按钮，点它下载。如果点了链接看到一堆乱码，就在链接上**右键 → 链接另存为**。

⚠️ 两个文件**必须放在同一个文件夹**，否则安装脚本找不到 APK。

### 方式 B：下载整个项目（含全部源码）

仓库页面右上角绿色 **「Code」→「Download ZIP」**，解压后所有文件都在里面。

---

## 第 ② 步：安装 ADB 工具

ADB 是安卓官方的调试工具，安装脚本要靠它来跟随身WiFi通信。

### Windows（最常见）

1. 打开官方下载页：**https://developer.android.com/tools/releases/platform-tools**
2. 往下翻到 **"Command line tools only"** 区域，点 **"SDK Platform-Tools for Windows"** 下载（是一个 ZIP 文件）
3. 把 ZIP 解压到一个**固定位置**，比如 `C:\platform-tools`
4. 把这个目录加到系统 PATH（让电脑认识 `adb` 命令）：

   - 按 **Win 键**，输入「环境变量」，打开「**编辑系统环境变量**」
   - 点右下角「**环境变量**」按钮
   - 在「系统变量」里找到 **Path**，双击
   - 点「**新建**」，粘贴 `C:\platform-tools`（你刚才解压的路径）
   - 一路「确定」保存

5. **验证**：按 **Win+R**，输入 `cmd` 回车，在黑窗口里输入：
   ```
   adb version
   ```
   如果显示类似 `Android Debug Bridge version 1.0.41`，说明装好了 ✅

> 如果显示「不是内部或外部命令」，说明 PATH 没加对，或者**之前打开了 cmd 窗口需要关掉重新开**才能生效。

### 免安装方案（不想配 PATH 的话）

把第①步下载的两个文件，**直接复制到 ADB 解压出来的文件夹里**（和 `adb.exe` 放一起）。然后在这个文件夹的地址栏输入 `cmd` 回车，就能直接用 adb 了。

---

## 第 ③ 步：随身WiFi 开启 USB 调试 + 连电脑

> M3H_ultra 这台设备基于安卓系统，需要开启「开发者选项」才能用 ADB。

### 3.1 开启「开发者选项」

1. 在随身WiFi上打开 **「设置」**
2. 找到 **「关于手机」**（或「关于设备」）
3. 连续点击 **「版本号」** 7 次（或「软件版本信息」）
4. 屏幕提示「**您现在处于开发者模式**」就成功了 ✅

### 3.2 开启「USB 调试」

1. 返回「设置」主页
2. 找到 **「系统」→「开发者选项」**（有的系统在「设置」最底部直接就有「开发者选项」）
3. 打开 **「USB 调试」** 开关
4. 屏幕弹出授权确认框时，勾选「**始终允许来自此计算机**」，点「**允许**」

### 3.3 用数据线连到电脑

1. 用 **数据线**（注意：必须是能传数据的线，有些便宜的线只能充电）把随身WiFi插到电脑 USB 口
2. 随身WiFi屏幕上如果弹窗「**是否允许 USB 调试？**」，点**允许**
3. （如果设备同时弹出「选择 USB 模式」，选「**文件传输 MTP**」或保持默认即可）

### 3.4 验证电脑能识别到设备

在电脑上打开 cmd（Win+R → 输入 `cmd` 回车），输入：
```
adb devices
```

如果显示类似下面这样（关键是要有设备序列号 + `device`）：
```
List of devices attached
M3H7540300025          device
```
说明连接成功 ✅

> ⚠️ 如果显示 `unauthorized`：随身WiFi屏幕上看有没有弹 USB 调试授权框，点允许。
>
> ⚠️ 如果列表是空的：换一根数据线、换个 USB 口，或者去设备管理器看看有没有未识别的驱动。

---

## 第 ④ 步：一键安装（核心步骤）

到这里，最难的部分都过去了。接下来双击一下就行。

1. 打开第 ① 步放 APK 和脚本的那个文件夹
2. **双击「一键安装.bat」**
3. 会弹出一个黑色窗口，自动执行 6 步操作：

```
[1/6] 检查设备连接...        ← 检测到随身WiFi
[2/6] 安装 APK...            ← 把应用装到设备上
[3/6] 授予悬浮窗权限...      ← 让手势条能显示
[4/6] 启用无障碍服务...      ← 让手势能触发返回/回桌面
[5/6] 隐藏导航栏...          ← 把系统底部虚拟键藏掉
[6/6] 启动手势服务...        ← 手势条出现
```

4. 看到 **「安装完成！手势导航已启用」** 就大功告成了 🎉

现在随身WiFi 屏幕底部应该能看到一条很细的横条（不仔细看可能看不到，这是正常的，只在手指划过时才会高亮）。试试下面这些手势：

| 手势 | 动作 |
|---|---|
| 从屏幕**最底部往上**快速划 | **回桌面** |
| 从最底部往上划到**一半停住**（约半秒） | **打开最近任务** |
| 从屏幕**左侧**或**右侧最边缘往里**划 | **返回** |

> 💡 提示：上滑和侧滑都要**从屏幕最边缘的玻璃边开始**划起，不是从屏幕中间划。新手可以稍微用力贴着边划。

---

## 🔧 如果一键脚本失败

### 情况 1：提示「未找到 adb」

说明 ADB 没装好或 PATH 没配，回到**第 ② 步**重做，或者用上面的「免安装方案」把文件复制到 adb.exe 旁边。

### 情况 2：提示「未检测到设备」

- 确认 USB 调试已开启（第③步）
- 在 cmd 里跑 `adb devices` 看看有没有设备
- 换数据线、换 USB 口
- 随身WiFi屏幕上的调试授权弹窗要点「允许」

### 情况 3：安装时提示「INSTALL_FAILED_UPDATE_INCOMPATIBLE」

设备上装过旧版本，签名不一致。先卸载旧的再装：
```
adb uninstall com.m3h.gesturenav
```
然后重新双击一键安装.bat。

### 情况 4：安装成功了但手势不灵

最常见原因是**无障碍服务没真正启用**。在 cmd 里执行（先清空再重设，能解决大部分问题）：
```
adb shell settings put secure enabled_accessibility_services ""
adb shell settings put secure accessibility_enabled 0
adb shell settings put secure enabled_accessibility_services com.m3h.gesturenav/com.m3h.gesturenav.GestureAccessibilityService
adb shell settings put secure accessibility_enabled 1
adb shell am start -n com.m3h.gesturenav/.MainActivity
```
等几秒钟，手势条就会出现。

---

## ❓ 常见疑问

**Q：每次用都要连电脑吗？**
不用。安装只需一次。之后随身WiFi独立运行，拔掉数据线照常使用。

**Q：重启随身WiFi后手势还有效吗？**
有效。本应用内置开机自启。但个别系统会限制后台自启，**如果重启后手势失灵，手动点一下 App 图标打开一次即可恢复**。

**Q：底部图标还能点吗？会不会被手势条挡住？**
不会。v1.1 版本手势条只占屏幕最底部 10 像素，第三方 App 的底部图标完全不受影响，正常点击。

**Q：想彻底卸载怎么办？**
连上电脑，在 cmd 里执行：
```
adb uninstall com.m3h.gesturenav
adb shell settings delete global policy_control
```
或者双击仓库里的「**一键卸载.bat**」。

**Q：这个应用安全吗？**
这是开源项目，全部源码在本仓库的 `src/` 目录，可自行审查。应用只通过安卓官方的无障碍接口实现手势，不联网、不上传任何数据。

---

## 📌 一句话总结

> 下载 APK + 一键安装.bat → 装 ADB → 开 USB 调试 → 连电脑 → 双击 bat → 完成。

有任何问题，欢迎在 GitHub 仓库提 Issue。
